import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const TxnInfo = ({navigation, txnDetails}) => {
  return <View></View>;
};

const styles = StyleSheet.create({
  txnInfo: {
    flex: 1,
  },
});

export default TxnInfo;
